# Tasks: Monitor Profile Camera Controls

- [x] Implementation
    - [x] Add `facing` and `flash` states to `ProfilePhotoCaptureModal.tsx`
    - [x] Implement `toggleFacing` and `toggleFlash` functions
    - [x] Add the top controls UI layer for Flash and Camera Flip
    - [x] Update `CameraView` props to use the new states
    - [x] Add necessary styles for the top controls
- [x] Verification
    - [x] Verify that the camera flips between front and back
    - [x] Verify that the flash mode toggles (Auto -> On -> Off)
    - [x] Ensure capture still works in both orientations
